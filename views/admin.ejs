<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Bo Noti</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: Arial, sans-serif; background: #1a1a2e; color: #eee; min-height: 100vh; }
        .navbar { background: #16213e; padding: 15px 30px; display: flex; justify-content: space-between; align-items: center; }
        .navbar h1 { font-size: 1.5em; color: #e94560; }
        .navbar a { color: #eee; text-decoration: none; padding: 8px 15px; background: #e94560; border-radius: 5px; }
        .navbar a:hover { background: #d63e57; }
        .container { max-width: 1200px; margin: 30px auto; padding: 0 20px; }
        
        /* Stats */
        .stats { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin-bottom: 30px; }
        .stat-card { background: #16213e; padding: 20px; border-radius: 10px; text-align: center; }
        .stat-card h3 { color: #e94560; font-size: 2em; }
        .stat-card p { color: #aaa; margin-top: 5px; }
        
        /* Sections */
        .section { background: #16213e; border-radius: 10px; padding: 20px; margin-bottom: 30px; }
        .section h2 { color: #e94560; margin-bottom: 20px; border-bottom: 1px solid #333; padding-bottom: 10px; }
        
        /* Form */
        .form-inline { display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 20px; }
        .form-inline input { flex: 1; min-width: 150px; padding: 10px; border: 1px solid #333; border-radius: 5px; background: #0f0f23; color: #eee; }
        .form-inline button { padding: 10px 20px; background: #28a745; color: white; border: none; border-radius: 5px; cursor: pointer; }
        .form-inline button:hover { background: #218838; }
        
        /* Table */
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #333; }
        th { background: #0f0f23; color: #e94560; }
        tr:hover { background: #0f0f23; }
        .btn-delete { padding: 5px 10px; background: #dc3545; color: white; border: none; border-radius: 3px; cursor: pointer; }
        .btn-delete:hover { background: #c82333; }
        
        .empty { text-align: center; padding: 20px; color: #666; }
        
        @media (max-width: 768px) {
            .stats { grid-template-columns: repeat(2, 1fr); }
        }
    </style>
</head>
<body>
    <div class="navbar">
        <h1>👑 Admin Dashboard</h1>
        <div>
            <span>👤 <%= user.username %></span>
            <a href="/auth/logout">Đăng xuất</a>
        </div>
    </div>
    
    <div class="container">
        <!-- Stats -->
        <div class="stats">
            <div class="stat-card">
                <h3><%= stats.totalUsers || 0 %></h3>
                <p>👥 Users</p>
            </div>
            <div class="stat-card">
                <h3><%= stats.totalProducts || 0 %></h3>
                <p>📦 Sản phẩm</p>
            </div>
            <div class="stat-card">
                <h3><%= stats.totalOrders || 0 %></h3>
                <p>🛒 Đơn hàng</p>
            </div>
            <div class="stat-card">
                <h3><%= (stats.totalRevenue || 0).toLocaleString('vi-VN') %></h3>
                <p>💰 Doanh thu (VND)</p>
            </div>
        </div>

        <!-- Thêm sản phẩm -->
        <div class="section">
            <h2>➕ Thêm sản phẩm mới</h2>
            <form action="/admin/product/add" method="POST" class="form-inline">
                <input type="text" name="name" placeholder="Tên sản phẩm" required>
                <input type="number" name="price" placeholder="Giá (VND)" required>
                <input type="text" name="description" placeholder="Mô tả">
                <input type="text" name="image" placeholder="Emoji (📦)" maxlength="5">
                <button type="submit">Thêm</button>
            </form>
        </div>

        <!-- Danh sách sản phẩm -->
        <div class="section">
            <h2>📦 Danh sách sản phẩm</h2>
            <% if (products.length === 0) { %>
                <p class="empty">Chưa có sản phẩm nào</p>
            <% } else { %>
                <table>
                    <tr><th>Icon</th><th>Tên</th><th>Giá</th><th>Mô tả</th><th>Xóa</th></tr>
                    <% products.forEach(function(p) { %>
                        <tr>
                            <td><%= p.image %></td>
                            <td><%= p.name %></td>
                            <td><%= p.price.toLocaleString('vi-VN') %> VND</td>
                            <td><%= p.description || '-' %></td>
                            <td>
                                <form action="/admin/product/delete/<%= p._id %>" method="POST" style="display:inline;">
                                    <button type="submit" class="btn-delete" onclick="return confirm('Xóa sản phẩm này?')">🗑️</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </table>
            <% } %>
        </div>

        <!-- Danh sách users -->
        <div class="section">
            <h2>👥 Danh sách Users</h2>
            <% if (users.length === 0) { %>
                <p class="empty">Chưa có user nào</p>
            <% } else { %>
                <table>
                    <tr><th>Username</th><th>Role</th><th>Ngày tạo</th><th>Xóa</th></tr>
                    <% users.forEach(function(u) { %>
                        <tr>
                            <td><%= u.username %></td>
                            <td><%= u.role === 'admin' ? '👑 Admin' : '👤 User' %></td>
                            <td><%= new Date(u.createdAt).toLocaleDateString('vi-VN') %></td>
                            <td>
                                <% if (u._id.toString() !== user.id.toString()) { %>
                                    <form action="/admin/user/delete/<%= u._id %>" method="POST" style="display:inline;">
                                        <button type="submit" class="btn-delete" onclick="return confirm('Xóa user này?')">🗑️</button>
                                    </form>
                                <% } else { %>
                                    -
                                <% } %>
                            </td>
                        </tr>
                    <% }); %>
                </table>
            <% } %>
        </div>

        <!-- Đơn hàng gần đây -->
        <div class="section">
            <h2>🛒 Đơn hàng gần đây</h2>
            <% if (orders.length === 0) { %>
                <p class="empty">Chưa có đơn hàng nào</p>
            <% } else { %>
                <table>
                    <tr><th>User</th><th>Sản phẩm</th><th>Số lượng</th><th>Tổng tiền</th><th>Thời gian</th></tr>
                    <% orders.slice(-10).reverse().forEach(function(o) { %>
                        <tr>
                            <td><%= o.user ? o.user.username : 'N/A' %></td>
                            <td><%= o.product ? o.product.name : 'N/A' %></td>
                            <td><%= o.quantity %></td>
                            <td><%= o.totalPrice.toLocaleString('vi-VN') %> VND</td>
                            <td><%= new Date(o.createdAt).toLocaleString('vi-VN') %></td>
                        </tr>
                    <% }); %>
                </table>
            <% } %>
        </div>
    </div>
</body>
</html>
